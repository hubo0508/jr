package com.jr.platform.product.service.test;

import org.junit.Test;

import com.ms.simple.core.BaseJunit;

public class ProductServiceTest extends BaseJunit {
	
	
	@Test
	public void queryProductList() {
		//productService.queryProductList(page, product_name, device_type_id, product_category, place_origin, model, material, exterior_size, effective_volume, product_weight, voltage, electric_current, power, energy, temperature_range, coolant, work_mode, capacity, stock, brand)
	}
}
